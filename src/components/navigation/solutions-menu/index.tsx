
import { MenuContainer } from "../menu-container";
import { SolutionsContent } from "./SolutionsContent";

export const SolutionsMenu = () => {
  return (
    <MenuContainer title="Solutions">
      <SolutionsContent />
    </MenuContainer>
  );
};
